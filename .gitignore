# 与 lobster 一致：密钥与本地配置
.env
.env.deploy
*.local
sutui_config.json
openclaw/.env
upstream_urls.json
custom_configs.json
models_config.json

# 数据库与运行时
*.db
*.db-journal
*.log
logs/
backend.log
mcp.log

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.pyo
.Python
*.so

# Node（可选）
node_modules/

# 运行时
browser_data/
browser_chromium/
assets/
installed_packages.json
mcp/capability_catalog.local.json

# OTA manifest（服务器独立维护，不随 git pull 覆盖）
client_static/client_code/manifest.json
client_static/skill_bundle/manifest.json
client_static/client_code/bundles/
client_static/skill_bundle/bundles/

# 服务器运行时数据
data/sutui_llm_catalog_from_api.json
data/sutui_llm_snapshot.json
data/sutui_reconcile_last.json
temp_assets/

# 落地页：私有安装包（仅 paid 订单凭 token 才能下载）不进 git，直接 scp 到服务器
landing_private/*.zip
landing_private/*.exe

# 部署回滚
.deploy_rollback_commit

# 系统/编辑器
.DS_Store
Thumbs.db
.idea/
.vscode/
